/**
 * @param {string} s
 * @param {string} goal
 * @return {boolean}
 */
 buddyStrings = function(s, goal) {
    if (s === goal) {
        return true;
    }
    console.log(s, goal)
};

buddyStrings('ab', 'ba')